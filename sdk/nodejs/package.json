{
    "name": "@jaxxstorm/pulumi-aws-securebaseline",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource securebaseline ${VERSION}"
    },
    "dependencies": {
        "@pulumi/aws": "^4.0.0"
    },
    "devDependencies": {
        "typescript": "^3.7.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    },
    "pulumi": {
        "resource": true,
        "pluginDownloadURL": "https://github.com/jaxxstorm/pulumi-aws-securebaseline/releases/download/v${VERSION}"
    }
}
